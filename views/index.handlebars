<center>
    <h1>WHO'S READY FOR SOME TASTY BURGERS?</h1>
</center>

    <h2>MENU</h2>

    <ul>
        {{#each burgers}}
            {{#unless devoured}}
                {{> burgers/burger-block devoured=true}}
            {{/unless}}
        {{/each}}
    </ul>

    <h2>Burgers that have been eaten.</h2>

    <ul>
        {{#each burgers}}
            {{#if devoured}}
                {{> burgers/burger-block devoured=false}}
            {{/if}}
        {{/each}}
    </ul>

    <img src="https://melbournechapter.net/images/cheeseburger-transparent-5.png" alt="">

    <h2>Add a Burger</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="ca">Burger Name:</label>
            <input type="text" id="ca" name="name">
        </div>

        <div class="form-group">
            <label for="dev">Devoured?</label><br>
            <input type="radio" name="devoured" value="1" checked> Devoured!<br>
            <input type="radio" name="devoured" value="0"> Uneaten
        </div>

        <button type="submit" id="submit-button">Add Burger</button>
    </form>